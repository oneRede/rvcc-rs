  # 数据段标签
  .data
.L..36:
  .byte 79	# 字符：O
  .byte 75	# 字符：K
  .byte 10
  .byte 0
  # 数据段标签
  .data
.L..35:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..34:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 121	# 字符：y
  .byte 61	# 字符：=
  .byte 52	# 字符：4
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..33:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..32:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..31:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..30:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 121	# 字符：y
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 121	# 字符：y
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..29:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 121	# 字符：y
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..28:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..27:
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..26:
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 103	# 字符：g
  .byte 49	# 字符：1
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..25:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 48	# 字符：0
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 49	# 字符：1
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 50	# 字符：2
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..24:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 48	# 字符：0
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 49	# 字符：1
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 50	# 字符：2
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 50	# 字符：2
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..23:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 48	# 字符：0
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 49	# 字符：1
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 50	# 字符：2
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 49	# 字符：1
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..22:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 48	# 字符：0
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 49	# 字符：1
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 50	# 字符：2
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 50	# 字符：2
  .byte 91	# 字符：[
  .byte 48	# 字符：0
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..21:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 49	# 字符：1
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 103	# 字符：g
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..20:
  .byte 103	# 字符：g
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..19:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..18:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..17:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 91	# 字符：[
  .byte 52	# 字符：4
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 42	# 字符：*
  .byte 42	# 字符：*
  .byte 120	# 字符：x
  .byte 32	# 字符： 
  .byte 43	# 字符：+
  .byte 32	# 字符： 
  .byte 49	# 字符：1
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..16:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 91	# 字符：[
  .byte 52	# 字符：4
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 32	# 字符： 
  .byte 42	# 字符：*
  .byte 42	# 字符：*
  .byte 120	# 字符：x
  .byte 32	# 字符： 
  .byte 43	# 字符：+
  .byte 32	# 字符： 
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..15:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 91	# 字符：[
  .byte 52	# 字符：4
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 42	# 字符：*
  .byte 42	# 字符：*
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 43	# 字符：+
  .byte 32	# 字符： 
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..14:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 91	# 字符：[
  .byte 52	# 字符：4
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 42	# 字符：*
  .byte 42	# 字符：*
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..13:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 91	# 字符：[
  .byte 52	# 字符：4
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 42	# 字符：*
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..12:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 51	# 字符：3
  .byte 93	# 字符：]
  .byte 91	# 字符：[
  .byte 52	# 字符：4
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..11:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 91	# 字符：[
  .byte 52	# 字符：4
  .byte 93	# 字符：]
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..10:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 42	# 字符：*
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..9:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..8:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 105	# 字符：i
  .byte 122	# 字符：z
  .byte 101	# 字符：e
  .byte 111	# 字符：o
  .byte 102	# 字符：f
  .byte 40	# 字符：(
  .byte 120	# 字符：x
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..7:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 102	# 字符：f
  .byte 111	# 字符：o
  .byte 111	# 字符：o
  .byte 49	# 字符：1
  .byte 50	# 字符：2
  .byte 51	# 字符：3
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 98	# 字符：b
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 61	# 字符：=
  .byte 53	# 字符：5
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 102	# 字符：f
  .byte 111	# 字符：o
  .byte 111	# 字符：o
  .byte 49	# 字符：1
  .byte 50	# 字符：2
  .byte 51	# 字符：3
  .byte 43	# 字符：+
  .byte 98	# 字符：b
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..6:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 102	# 字符：f
  .byte 111	# 字符：o
  .byte 111	# 字符：o
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 102	# 字符：f
  .byte 111	# 字符：o
  .byte 111	# 字符：o
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..5:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 98	# 字符：b
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 61	# 字符：=
  .byte 98	# 字符：b
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 43	# 字符：+
  .byte 98	# 字符：b
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..4:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 122	# 字符：z
  .byte 61	# 字符：=
  .byte 53	# 字符：5
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 43	# 字符：+
  .byte 122	# 字符：z
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..3:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..2:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 122	# 字符：z
  .byte 61	# 字符：=
  .byte 53	# 字符：5
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 43	# 字符：+
  .byte 122	# 字符：z
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..1:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..0:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 97	# 字符：a
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
  # 全局段g2
  .globl g2
g2:
  # 全局变量零填充32位
  .zero 32
  # 数据段标签
  .data
  # 全局段g1
  .globl g1
g1:
  # 全局变量零填充8位
  .zero 8

  # 定义全局main段
  .globl main
  # 代码段标签
  .text
# =====main段开始===============
# main段标签
main:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -816

# =====段主体===============
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-808(fp)
  addi a0, fp, -808
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量a的栈内地址为-808(fp)
  addi a0, fp, -808
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..0的地址
  la a0, .L..0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-800(fp)
  addi a0, fp, -800
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量a的栈内地址为-800(fp)
  addi a0, fp, -800
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..1的地址
  la a0, .L..1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-792(fp)
  addi a0, fp, -792
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量z的栈内地址为-784(fp)
  addi a0, fp, -784
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量z的栈内地址为-784(fp)
  addi a0, fp, -784
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-792(fp)
  addi a0, fp, -792
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..2的地址
  la a0, .L..2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-776(fp)
  addi a0, fp, -776
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量a的栈内地址为-776(fp)
  addi a0, fp, -776
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..3的地址
  la a0, .L..3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-768(fp)
  addi a0, fp, -768
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量z的栈内地址为-760(fp)
  addi a0, fp, -760
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量z的栈内地址为-760(fp)
  addi a0, fp, -760
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-768(fp)
  addi a0, fp, -768
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..4的地址
  la a0, .L..4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将6加载到a0中
  li a0, 6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-752(fp)
  addi a0, fp, -752
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量b的栈内地址为-744(fp)
  addi a0, fp, -744
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量b的栈内地址为-744(fp)
  addi a0, fp, -744
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为-752(fp)
  addi a0, fp, -752
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..5的地址
  la a0, .L..5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量foo的栈内地址为-736(fp)
  addi a0, fp, -736
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量foo的栈内地址为-736(fp)
  addi a0, fp, -736
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..6的地址
  la a0, .L..6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量foo123的栈内地址为-728(fp)
  addi a0, fp, -728
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量bar的栈内地址为-720(fp)
  addi a0, fp, -720
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量bar的栈内地址为-720(fp)
  addi a0, fp, -720
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量foo123的栈内地址为-728(fp)
  addi a0, fp, -728
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..7的地址
  la a0, .L..7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..8的地址
  la a0, .L..8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..9的地址
  la a0, .L..9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..10的地址
  la a0, .L..10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将32加载到a0中
  li a0, 32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将32加载到a0中
  li a0, 32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..11的地址
  la a0, .L..11
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将96加载到a0中
  li a0, 96
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将96加载到a0中
  li a0, 96
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..12的地址
  la a0, .L..12
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将32加载到a0中
  li a0, 32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将32加载到a0中
  li a0, 32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..13的地址
  la a0, .L..13
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..14的地址
  la a0, .L..14
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将9加载到a0中
  li a0, 9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..15的地址
  la a0, .L..15
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将9加载到a0中
  li a0, 9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..16的地址
  la a0, .L..16
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..17的地址
  la a0, .L..17
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-80(fp)
  addi a0, fp, -80
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..18的地址
  la a0, .L..18
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-72(fp)
  addi a0, fp, -72
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 获取局部变量x的栈内地址为-72(fp)
  addi a0, fp, -72
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..19的地址
  la a0, .L..19
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g1的地址
  la a0, g1
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..20的地址
  la a0, .L..20
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g1的地址
  la a0, g1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取全局变量g1的地址
  la a0, g1
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..21的地址
  la a0, .L..21
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..22的地址
  la a0, .L..22
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..23的地址
  la a0, .L..23
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..24的地址
  la a0, .L..24
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量g2的地址
  la a0, g2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..25的地址
  la a0, .L..25
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..26的地址
  la a0, .L..26
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将32加载到a0中
  li a0, 32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将32加载到a0中
  li a0, 32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..27的地址
  la a0, .L..27
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-64(fp)
  addi a0, fp, -64
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sb a0, 0(a1)
  # 获取局部变量x的栈内地址为-64(fp)
  addi a0, fp, -64
  # 读取a0中存放的地址,得到的值存入a0
  lb a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..28的地址
  la a0, .L..28
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-63(fp)
  addi a0, fp, -63
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sb a0, 0(a1)
  # 获取局部变量y的栈内地址为-62(fp)
  addi a0, fp, -62
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sb a0, 0(a1)
  # 获取局部变量x的栈内地址为-63(fp)
  addi a0, fp, -63
  # 读取a0中存放的地址,得到的值存入a0
  lb a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..29的地址
  la a0, .L..29
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-61(fp)
  addi a0, fp, -61
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sb a0, 0(a1)
  # 获取局部变量y的栈内地址为-60(fp)
  addi a0, fp, -60
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sb a0, 0(a1)
  # 获取局部变量y的栈内地址为-60(fp)
  addi a0, fp, -60
  # 读取a0中存放的地址,得到的值存入a0
  lb a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..30的地址
  la a0, .L..30
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..31的地址
  la a0, .L..31
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..32的地址
  la a0, .L..32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-48(fp)
  addi a0, fp, -48
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量x的栈内地址为-40(fp)
  addi a0, fp, -40
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量x的栈内地址为-48(fp)
  addi a0, fp, -48
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..33的地址
  la a0, .L..33
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-32(fp)
  addi a0, fp, -32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量x的栈内地址为-24(fp)
  addi a0, fp, -24
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量y的栈内地址为-16(fp)
  addi a0, fp, -16
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将4加载到a0中
  li a0, 4
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量x的栈内地址为-32(fp)
  addi a0, fp, -32
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..34的地址
  la a0, .L..34
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为-8(fp)
  addi a0, fp, -8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量x的栈内地址为-8(fp)
  addi a0, fp, -8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量x的栈内地址为-8(fp)
  addi a0, fp, -8
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..35的地址
  la a0, .L..35
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 获取全局变量.L..36的地址
  la a0, .L..36
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数printf
  call printf
# 返回语句
  # 将0加载到a0中
  li a0, 0
  # 跳转到.L.return.main段
  j .L.return.main

# =====段结束===============
# return段标签
.L.return.main:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret
