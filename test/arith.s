  # 数据段标签
  .data
.L..25:
  .byte 79	# 字符：O
  .byte 75	# 字符：K
  .byte 10
  .byte 0
  # 数据段标签
  .data
.L..24:
  .byte 49	# 字符：1
  .byte 62	# 字符：>
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 0
  # 数据段标签
  .data
.L..23:
  .byte 49	# 字符：1
  .byte 62	# 字符：>
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..22:
  .byte 49	# 字符：1
  .byte 62	# 字符：>
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 0
  # 数据段标签
  .data
.L..21:
  .byte 49	# 字符：1
  .byte 62	# 字符：>
  .byte 50	# 字符：2
  .byte 0
  # 数据段标签
  .data
.L..20:
  .byte 49	# 字符：1
  .byte 62	# 字符：>
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..19:
  .byte 49	# 字符：1
  .byte 62	# 字符：>
  .byte 48	# 字符：0
  .byte 0
  # 数据段标签
  .data
.L..18:
  .byte 50	# 字符：2
  .byte 60	# 字符：<
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..17:
  .byte 49	# 字符：1
  .byte 60	# 字符：<
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..16:
  .byte 48	# 字符：0
  .byte 60	# 字符：<
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..15:
  .byte 50	# 字符：2
  .byte 60	# 字符：<
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..14:
  .byte 49	# 字符：1
  .byte 60	# 字符：<
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..13:
  .byte 48	# 字符：0
  .byte 60	# 字符：<
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..12:
  .byte 52	# 字符：4
  .byte 50	# 字符：2
  .byte 33	# 字符：!
  .byte 61	# 字符：=
  .byte 52	# 字符：4
  .byte 50	# 字符：2
  .byte 0
  # 数据段标签
  .data
.L..11:
  .byte 48	# 字符：0
  .byte 33	# 字符：!
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..10:
  .byte 52	# 字符：4
  .byte 50	# 字符：2
  .byte 61	# 字符：=
  .byte 61	# 字符：=
  .byte 52	# 字符：4
  .byte 50	# 字符：2
  .byte 0
  # 数据段标签
  .data
.L..9:
  .byte 48	# 字符：0
  .byte 61	# 字符：=
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 0
  # 数据段标签
  .data
.L..8:
  .byte 45	# 字符：-
  .byte 32	# 字符： 
  .byte 45	# 字符：-
  .byte 32	# 字符： 
  .byte 43	# 字符：+
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 0
  # 数据段标签
  .data
.L..7:
  .byte 45	# 字符：-
  .byte 32	# 字符： 
  .byte 45	# 字符：-
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 0
  # 数据段标签
  .data
.L..6:
  .byte 40	# 字符：(
  .byte 51	# 字符：3
  .byte 43	# 字符：+
  .byte 53	# 字符：5
  .byte 41	# 字符：)
  .byte 47	# 字符：/
  .byte 50	# 字符：2
  .byte 0
  # 数据段标签
  .data
.L..5:
  .byte 53	# 字符：5
  .byte 42	# 字符：*
  .byte 40	# 字符：(
  .byte 57	# 字符：9
  .byte 45	# 字符：-
  .byte 54	# 字符：6
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..4:
  .byte 53	# 字符：5
  .byte 43	# 字符：+
  .byte 54	# 字符：6
  .byte 42	# 字符：*
  .byte 55	# 字符：7
  .byte 0
  # 数据段标签
  .data
.L..3:
  .byte 49	# 字符：1
  .byte 50	# 字符：2
  .byte 32	# 字符： 
  .byte 43	# 字符：+
  .byte 32	# 字符： 
  .byte 51	# 字符：3
  .byte 52	# 字符：4
  .byte 32	# 字符： 
  .byte 45	# 字符：-
  .byte 32	# 字符： 
  .byte 53	# 字符：5
  .byte 0
  # 数据段标签
  .data
.L..2:
  .byte 53	# 字符：5
  .byte 43	# 字符：+
  .byte 50	# 字符：2
  .byte 48	# 字符：0
  .byte 45	# 字符：-
  .byte 52	# 字符：4
  .byte 0
  # 数据段标签
  .data
.L..1:
  .byte 52	# 字符：4
  .byte 50	# 字符：2
  .byte 0
  # 数据段标签
  .data
.L..0:
  .byte 48	# 字符：0
  .byte 0

  # 定义全局main段
  .globl main
  # 代码段标签
  .text
# =====main段开始===============
# main段标签
main:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0

# =====段主体===============
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..0的地址
  la a0, .L..0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将42加载到a0中
  li a0, 42
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将42加载到a0中
  li a0, 42
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..1的地址
  la a0, .L..1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将21加载到a0中
  li a0, 21
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将4加载到a0中
  li a0, 4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将20加载到a0中
  li a0, 20
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..2的地址
  la a0, .L..2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将41加载到a0中
  li a0, 41
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将34加载到a0中
  li a0, 34
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将12加载到a0中
  li a0, 12
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..3的地址
  la a0, .L..3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将47加载到a0中
  li a0, 47
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将7加载到a0中
  li a0, 7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将6加载到a0中
  li a0, 6
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..4的地址
  la a0, .L..4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将15加载到a0中
  li a0, 15
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将6加载到a0中
  li a0, 6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将9加载到a0中
  li a0, 9
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0*a1,结果写入a0
  mul a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..5的地址
  la a0, .L..5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将4加载到a0中
  li a0, 4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0÷a1,结果写入a0
  div a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..6的地址
  la a0, .L..6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将10加载到a0中
  li a0, 10
  # 对a0值进行取反
  neg a0, a0
  # 对a0值进行取反
  neg a0, a0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..7的地址
  la a0, .L..7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将10加载到a0中
  li a0, 10
  # 对a0值进行取反
  neg a0, a0
  # 对a0值进行取反
  neg a0, a0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..8的地址
  la a0, .L..8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  xor a0, a0, a1
  seqz a0, a0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..9的地址
  la a0, .L..9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将42加载到a0中
  li a0, 42
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将42加载到a0中
  li a0, 42
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  xor a0, a0, a1
  seqz a0, a0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..10的地址
  la a0, .L..10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0!=a1
  xor a0, a0, a1
  snez a0, a0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..11的地址
  la a0, .L..11
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将42加载到a0中
  li a0, 42
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将42加载到a0中
  li a0, 42
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0!=a1
  xor a0, a0, a1
  snez a0, a0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..12的地址
  la a0, .L..12
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..13的地址
  la a0, .L..13
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..14的地址
  la a0, .L..14
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..15的地址
  la a0, .L..15
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..16的地址
  la a0, .L..16
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..17的地址
  la a0, .L..17
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..18的地址
  la a0, .L..18
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..19的地址
  la a0, .L..19
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..20的地址
  la a0, .L..20
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..21的地址
  la a0, .L..21
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..22的地址
  la a0, .L..22
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..23的地址
  la a0, .L..23
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将0加载到a0中
  li a0, 0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..24的地址
  la a0, .L..24
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 获取全局变量.L..25的地址
  la a0, .L..25
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数printf
  call printf
# 返回语句
  # 将0加载到a0中
  li a0, 0
  # 跳转到.L.return.main段
  j .L.return.main

# =====段结束===============
# return段标签
.L.return.main:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret
