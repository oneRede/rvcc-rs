  # 数据段标签
  .data
.L..10:
  .byte 79	# 字符：O
  .byte 75	# 字符：K
  .byte 10
  .byte 0
  # 数据段标签
  .data
.L..9:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 106	# 字符：j
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 119	# 字符：w
  .byte 104	# 字符：h
  .byte 105	# 字符：i
  .byte 108	# 字符：l
  .byte 101	# 字符：e
  .byte 40	# 字符：(
  .byte 105	# 字符：i
  .byte 60	# 字符：<
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 123	# 字符：{
  .byte 106	# 字符：j
  .byte 61	# 字符：=
  .byte 105	# 字符：i
  .byte 43	# 字符：+
  .byte 106	# 字符：j
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 105	# 字符：i
  .byte 43	# 字符：+
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 125	# 字符：}
  .byte 32	# 字符： 
  .byte 106	# 字符：j
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..8:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 119	# 字符：w
  .byte 104	# 字符：h
  .byte 105	# 字符：i
  .byte 108	# 字符：l
  .byte 101	# 字符：e
  .byte 40	# 字符：(
  .byte 105	# 字符：i
  .byte 60	# 字符：<
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 105	# 字符：i
  .byte 43	# 字符：+
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..7:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 59	# 字符：;
  .byte 59	# 字符：;
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 53	# 字符：5
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..6:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 123	# 字符：{
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 125	# 字符：}
  .byte 32	# 字符： 
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..5:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 119	# 字符：w
  .byte 104	# 字符：h
  .byte 105	# 字符：i
  .byte 108	# 字符：l
  .byte 101	# 字符：e
  .byte 40	# 字符：(
  .byte 105	# 字符：i
  .byte 60	# 字符：<
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 105	# 字符：i
  .byte 43	# 字符：+
  .byte 49	# 字符：1
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..4:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 106	# 字符：j
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 102	# 字符：f
  .byte 111	# 字符：o
  .byte 114	# 字符：r
  .byte 32	# 字符： 
  .byte 40	# 字符：(
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 60	# 字符：<
  .byte 61	# 字符：=
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 61	# 字符：=
  .byte 105	# 字符：i
  .byte 43	# 字符：+
  .byte 49	# 字符：1
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 106	# 字符：j
  .byte 61	# 字符：=
  .byte 105	# 字符：i
  .byte 43	# 字符：+
  .byte 106	# 字符：j
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 106	# 字符：j
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..3:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 102	# 字符：f
  .byte 32	# 字符： 
  .byte 40	# 字符：(
  .byte 50	# 字符：2
  .byte 45	# 字符：-
  .byte 49	# 字符：1
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 101	# 字符：e
  .byte 108	# 字符：l
  .byte 115	# 字符：s
  .byte 101	# 字符：e
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..2:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 102	# 字符：f
  .byte 32	# 字符： 
  .byte 40	# 字符：(
  .byte 49	# 字符：1
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 101	# 字符：e
  .byte 108	# 字符：l
  .byte 115	# 字符：s
  .byte 101	# 字符：e
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..1:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 102	# 字符：f
  .byte 32	# 字符： 
  .byte 40	# 字符：(
  .byte 49	# 字符：1
  .byte 45	# 字符：-
  .byte 49	# 字符：1
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 101	# 字符：e
  .byte 108	# 字符：l
  .byte 115	# 字符：s
  .byte 101	# 字符：e
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..0:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 110	# 字符：n
  .byte 116	# 字符：t
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 105	# 字符：i
  .byte 102	# 字符：f
  .byte 32	# 字符： 
  .byte 40	# 字符：(
  .byte 48	# 字符：0
  .byte 41	# 字符：)
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 50	# 字符：2
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 101	# 字符：e
  .byte 108	# 字符：l
  .byte 115	# 字符：s
  .byte 101	# 字符：e
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 61	# 字符：=
  .byte 51	# 字符：3
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 120	# 字符：x
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0

  # 定义全局main段
  .globl main
  # 代码段标签
  .text
# =====main段开始===============
# main段标签
main:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -80

# =====段主体===============
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)

# =====分支语句2==============

# Cond表达式2
  # 将0加载到a0中
  li a0, 0
  # 若a0为0,则跳转到分支2的.L.else.2段
  beqz a0, .L.else.2

# Then语句2
  # 获取局部变量x的栈内地址为-80(fp)
  addi a0, fp, -80
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到分支2的.L.end.2段

  j .L.end.2

# Else语句2
# 分支2的.L.else.2段标签
.L.else.2:
  # 获取局部变量x的栈内地址为-80(fp)
  addi a0, fp, -80
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# 分支2的.L.end.2段标签
.L.end.2:
  # 获取局部变量x的栈内地址为-80(fp)
  addi a0, fp, -80
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..0的地址
  la a0, .L..0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)

# =====分支语句3==============

# Cond表达式3
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 若a0为0,则跳转到分支3的.L.else.3段
  beqz a0, .L.else.3

# Then语句3
  # 获取局部变量x的栈内地址为-72(fp)
  addi a0, fp, -72
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到分支3的.L.end.3段

  j .L.end.3

# Else语句3
# 分支3的.L.else.3段标签
.L.else.3:
  # 获取局部变量x的栈内地址为-72(fp)
  addi a0, fp, -72
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# 分支3的.L.end.3段标签
.L.end.3:
  # 获取局部变量x的栈内地址为-72(fp)
  addi a0, fp, -72
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..1的地址
  la a0, .L..1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)

# =====分支语句4==============

# Cond表达式4
  # 将1加载到a0中
  li a0, 1
  # 若a0为0,则跳转到分支4的.L.else.4段
  beqz a0, .L.else.4

# Then语句4
  # 获取局部变量x的栈内地址为-64(fp)
  addi a0, fp, -64
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到分支4的.L.end.4段

  j .L.end.4

# Else语句4
# 分支4的.L.else.4段标签
.L.else.4:
  # 获取局部变量x的栈内地址为-64(fp)
  addi a0, fp, -64
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# 分支4的.L.end.4段标签
.L.end.4:
  # 获取局部变量x的栈内地址为-64(fp)
  addi a0, fp, -64
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..2的地址
  la a0, .L..2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)

# =====分支语句5==============

# Cond表达式5
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 若a0为0,则跳转到分支5的.L.else.5段
  beqz a0, .L.else.5

# Then语句5
  # 获取局部变量x的栈内地址为-56(fp)
  addi a0, fp, -56
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到分支5的.L.end.5段

  j .L.end.5

# Else语句5
# 分支5的.L.else.5段标签
.L.else.5:
  # 获取局部变量x的栈内地址为-56(fp)
  addi a0, fp, -56
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# 分支5的.L.end.5段标签
.L.end.5:
  # 获取局部变量x的栈内地址为-56(fp)
  addi a0, fp, -56
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..3的地址
  la a0, .L..3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将55加载到a0中
  li a0, 55
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-48(fp)
  addi a0, fp, -48
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量j的栈内地址为-40(fp)
  addi a0, fp, -40
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# =====循环语句6===============

# Init语句%6
  # 获取局部变量i的栈内地址为-48(fp)
  addi a0, fp, -48
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# 循环6的.L.begin.6段标签
.L.begin.6:
# Cond表达式6
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-48(fp)
  addi a0, fp, -48
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 若a0为0,则跳转到循环6的.L.end.6段
  beqz a0, .L.end.6

# Then语句6
  # 获取局部变量j的栈内地址为-40(fp)
  addi a0, fp, -40
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量j的栈内地址为-40(fp)
  addi a0, fp, -40
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-48(fp)
  addi a0, fp, -48
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# Inc语句6
  # 获取局部变量i的栈内地址为-48(fp)
  addi a0, fp, -48
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-48(fp)
  addi a0, fp, -48
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到循环6的.L.begin.6段
  j .L.begin.6

# 循环6的.L.end.6段标签
.L.end.6:
  # 获取局部变量j的栈内地址为-40(fp)
  addi a0, fp, -40
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..4的地址
  la a0, .L..4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-32(fp)
  addi a0, fp, -32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# =====循环语句7===============

# 循环7的.L.begin.7段标签
.L.begin.7:
# Cond表达式7
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-32(fp)
  addi a0, fp, -32
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 若a0为0,则跳转到循环7的.L.end.7段
  beqz a0, .L.end.7

# Then语句7
  # 获取局部变量i的栈内地址为-32(fp)
  addi a0, fp, -32
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-32(fp)
  addi a0, fp, -32
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到循环7的.L.begin.7段
  j .L.begin.7

# 循环7的.L.end.7段标签
.L.end.7:
  # 获取局部变量i的栈内地址为-32(fp)
  addi a0, fp, -32
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..5的地址
  la a0, .L..5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 将2加载到a0中
  li a0, 2
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..6的地址
  la a0, .L..6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..7的地址
  la a0, .L..7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-24(fp)
  addi a0, fp, -24
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# =====循环语句8===============

# 循环8的.L.begin.8段标签
.L.begin.8:
# Cond表达式8
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-24(fp)
  addi a0, fp, -24
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断a0<a1
  slt a0, a0, a1
  # 若a0为0,则跳转到循环8的.L.end.8段
  beqz a0, .L.end.8

# Then语句8
  # 获取局部变量i的栈内地址为-24(fp)
  addi a0, fp, -24
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-24(fp)
  addi a0, fp, -24
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到循环8的.L.begin.8段
  j .L.begin.8

# 循环8的.L.end.8段标签
.L.end.8:
  # 获取局部变量i的栈内地址为-24(fp)
  addi a0, fp, -24
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..8的地址
  la a0, .L..8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将55加载到a0中
  li a0, 55
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-16(fp)
  addi a0, fp, -16
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量j的栈内地址为-8(fp)
  addi a0, fp, -8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将0加载到a0中
  li a0, 0
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)

# =====循环语句9===============

# 循环9的.L.begin.9段标签
.L.begin.9:
# Cond表达式9
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-16(fp)
  addi a0, fp, -16
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 若a0为0,则跳转到循环9的.L.end.9段
  beqz a0, .L.end.9

# Then语句9
  # 获取局部变量j的栈内地址为-8(fp)
  addi a0, fp, -8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量j的栈内地址为-8(fp)
  addi a0, fp, -8
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-16(fp)
  addi a0, fp, -16
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 获取局部变量i的栈内地址为-16(fp)
  addi a0, fp, -16
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量i的栈内地址为-16(fp)
  addi a0, fp, -16
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 将a0的值,写入到a1中存放的地址
  sd a0, 0(a1)
  # 跳转到循环9的.L.begin.9段
  j .L.begin.9

# 循环9的.L.end.9段标签
.L.end.9:
  # 获取局部变量j的栈内地址为-8(fp)
  addi a0, fp, -8
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..9的地址
  la a0, .L..9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 获取全局变量.L..10的地址
  la a0, .L..10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数printf
  call printf
# 返回语句
  # 将0加载到a0中
  li a0, 0
  # 跳转到.L.return.main段
  j .L.return.main

# =====段结束===============
# return段标签
.L.return.main:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret
