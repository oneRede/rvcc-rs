  # 数据段标签
  .data
.L..10:
  .byte 79	# 字符：O
  .byte 75	# 字符：K
  .byte 10
  .byte 0
  # 数据段标签
  .data
.L..9:
  .byte 40	# 字符：(
  .byte 123	# 字符：{
  .byte 32	# 字符： 
  .byte 115	# 字符：s
  .byte 117	# 字符：u
  .byte 98	# 字符：b
  .byte 95	# 字符：_
  .byte 99	# 字符：c
  .byte 104	# 字符：h
  .byte 97	# 字符：a
  .byte 114	# 字符：r
  .byte 40	# 字符：(
  .byte 55	# 字符：7
  .byte 44	# 字符：,
  .byte 32	# 字符： 
  .byte 51	# 字符：3
  .byte 44	# 字符：,
  .byte 32	# 字符： 
  .byte 51	# 字符：3
  .byte 41	# 字符：)
  .byte 59	# 字符：;
  .byte 32	# 字符： 
  .byte 125	# 字符：}
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..8:
  .byte 102	# 字符：f
  .byte 105	# 字符：i
  .byte 98	# 字符：b
  .byte 40	# 字符：(
  .byte 57	# 字符：9
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..7:
  .byte 115	# 字符：s
  .byte 117	# 字符：u
  .byte 98	# 字符：b
  .byte 50	# 字符：2
  .byte 40	# 字符：(
  .byte 52	# 字符：4
  .byte 44	# 字符：,
  .byte 51	# 字符：3
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..6:
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 50	# 字符：2
  .byte 40	# 字符：(
  .byte 51	# 字符：3
  .byte 44	# 字符：,
  .byte 52	# 字符：4
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..5:
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 54	# 字符：6
  .byte 40	# 字符：(
  .byte 49	# 字符：1
  .byte 44	# 字符：,
  .byte 50	# 字符：2
  .byte 44	# 字符：,
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 54	# 字符：6
  .byte 40	# 字符：(
  .byte 51	# 字符：3
  .byte 44	# 字符：,
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 54	# 字符：6
  .byte 40	# 字符：(
  .byte 52	# 字符：4
  .byte 44	# 字符：,
  .byte 53	# 字符：5
  .byte 44	# 字符：,
  .byte 54	# 字符：6
  .byte 44	# 字符：,
  .byte 55	# 字符：7
  .byte 44	# 字符：,
  .byte 56	# 字符：8
  .byte 44	# 字符：,
  .byte 57	# 字符：9
  .byte 41	# 字符：)
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 49	# 字符：1
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 50	# 字符：2
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 51	# 字符：3
  .byte 41	# 字符：)
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 52	# 字符：4
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 53	# 字符：5
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 54	# 字符：6
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..4:
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 54	# 字符：6
  .byte 40	# 字符：(
  .byte 49	# 字符：1
  .byte 44	# 字符：,
  .byte 50	# 字符：2
  .byte 44	# 字符：,
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 54	# 字符：6
  .byte 40	# 字符：(
  .byte 51	# 字符：3
  .byte 44	# 字符：,
  .byte 52	# 字符：4
  .byte 44	# 字符：,
  .byte 53	# 字符：5
  .byte 44	# 字符：,
  .byte 54	# 字符：6
  .byte 44	# 字符：,
  .byte 55	# 字符：7
  .byte 44	# 字符：,
  .byte 56	# 字符：8
  .byte 41	# 字符：)
  .byte 44	# 字符：,
  .byte 57	# 字符：9
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 48	# 字符：0
  .byte 44	# 字符：,
  .byte 49	# 字符：1
  .byte 49	# 字符：1
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..3:
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 54	# 字符：6
  .byte 40	# 字符：(
  .byte 49	# 字符：1
  .byte 44	# 字符：,
  .byte 50	# 字符：2
  .byte 44	# 字符：,
  .byte 51	# 字符：3
  .byte 44	# 字符：,
  .byte 52	# 字符：4
  .byte 44	# 字符：,
  .byte 53	# 字符：5
  .byte 44	# 字符：,
  .byte 54	# 字符：6
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..2:
  .byte 115	# 字符：s
  .byte 117	# 字符：u
  .byte 98	# 字符：b
  .byte 50	# 字符：2
  .byte 40	# 字符：(
  .byte 53	# 字符：5
  .byte 44	# 字符：,
  .byte 32	# 字符： 
  .byte 51	# 字符：3
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..1:
  .byte 97	# 字符：a
  .byte 100	# 字符：d
  .byte 100	# 字符：d
  .byte 50	# 字符：2
  .byte 40	# 字符：(
  .byte 51	# 字符：3
  .byte 44	# 字符：,
  .byte 32	# 字符： 
  .byte 53	# 字符：5
  .byte 41	# 字符：)
  .byte 0
  # 数据段标签
  .data
.L..0:
  .byte 114	# 字符：r
  .byte 101	# 字符：e
  .byte 116	# 字符：t
  .byte 51	# 字符：3
  .byte 40	# 字符：(
  .byte 41	# 字符：)
  .byte 0

  # 定义全局main段
  .globl main
  # 代码段标签
  .text
# =====main段开始===============
# main段标签
main:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0

# =====段主体===============
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)

  # 调用函数ret3
  call ret3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..0的地址
  la a0, .L..0
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add2
  call add2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..1的地址
  la a0, .L..1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数sub2
  call sub2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..2的地址
  la a0, .L..2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将21加载到a0中
  li a0, 21
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将4加载到a0中
  li a0, 4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将6加载到a0中
  li a0, 6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a5
  ld a5, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a4
  ld a4, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a3
  ld a3, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add6
  call add6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..3的地址
  la a0, .L..3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将66加载到a0中
  li a0, 66
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将4加载到a0中
  li a0, 4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将6加载到a0中
  li a0, 6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将7加载到a0中
  li a0, 7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a5
  ld a5, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a4
  ld a4, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a3
  ld a3, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add6
  call add6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将9加载到a0中
  li a0, 9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将11加载到a0中
  li a0, 11
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a5
  ld a5, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a4
  ld a4, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a3
  ld a3, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add6
  call add6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..4的地址
  la a0, .L..4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将136加载到a0中
  li a0, 136
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将4加载到a0中
  li a0, 4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将5加载到a0中
  li a0, 5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将6加载到a0中
  li a0, 6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将7加载到a0中
  li a0, 7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将8加载到a0中
  li a0, 8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将9加载到a0中
  li a0, 9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a5
  ld a5, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a4
  ld a4, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a3
  ld a3, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add6
  call add6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将10加载到a0中
  li a0, 10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将11加载到a0中
  li a0, 11
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将12加载到a0中
  li a0, 12
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将13加载到a0中
  li a0, 13
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a5
  ld a5, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a4
  ld a4, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a3
  ld a3, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add6
  call add6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将14加载到a0中
  li a0, 14
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将15加载到a0中
  li a0, 15
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将16加载到a0中
  li a0, 16
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a5
  ld a5, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a4
  ld a4, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a3
  ld a3, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add6
  call add6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..5的地址
  la a0, .L..5
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将7加载到a0中
  li a0, 7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将4加载到a0中
  li a0, 4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数add2
  call add2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..6的地址
  la a0, .L..6
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将4加载到a0中
  li a0, 4
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数sub2
  call sub2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..7的地址
  la a0, .L..7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将55加载到a0中
  li a0, 55
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将9加载到a0中
  li a0, 9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数fib
  call fib
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..8的地址
  la a0, .L..8
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将7加载到a0中
  li a0, 7
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将3加载到a0中
  li a0, 3
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数sub_char
  call sub_char
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取全局变量.L..9的地址
  la a0, .L..9
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a2
  ld a2, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数assert
  call assert
  # 获取全局变量.L..10的地址
  la a0, .L..10
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数printf
  call printf
# 返回语句
  # 将0加载到a0中
  li a0, 0
  # 跳转到.L.return.main段
  j .L.return.main

# =====段结束===============
# return段标签
.L.return.main:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret

  # 定义全局fib段
  .globl fib
  # 代码段标签
  .text
# =====fib段开始===============
# fib段标签
fib:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0
  # 将a0寄存器的值存入x的栈地址
  sd a0, 0(fp)

# =====段主体===============

# =====分支语句2==============

# Cond表达式2
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # 判断是否a0≤a1
  slt a0, a1, a0
  xori a0, a0, 1
  # 若a0为0,则跳转到分支2的.L.else.2段
  beqz a0, .L.else.2

# Then语句2
# 返回语句
  # 将1加载到a0中
  li a0, 1
  # 跳转到.L.return.fib段
  j .L.return.fib
  # 跳转到分支2的.L.end.2段

  j .L.end.2

# Else语句2
# 分支2的.L.else.2段标签
.L.else.2:

# 分支2的.L.end.2段标签
.L.end.2:
# 返回语句
  # 将2加载到a0中
  li a0, 2
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数fib
  call fib
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 将1加载到a0中
  li a0, 1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 弹栈，将栈顶的值存入a0
  ld a0, 0(sp)
  addi sp, sp, 8

  # 调用函数fib
  call fib
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 跳转到.L.return.fib段
  j .L.return.fib

# =====段结束===============
# return段标签
.L.return.fib:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret

  # 定义全局sub_char段
  .globl sub_char
  # 代码段标签
  .text
# =====sub_char段开始===============
# sub_char段标签
sub_char:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0
  # 将a0寄存器的值存入a的栈地址
  sb a0, 0(fp)
  # 将a1寄存器的值存入b的栈地址
  sb a1, 0(fp)
  # 将a2寄存器的值存入c的栈地址
  sb a2, 0(fp)

# =====段主体===============
# 返回语句
  # 获取局部变量c的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  lb a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量b的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  lb a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  lb a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 跳转到.L.return.sub_char段
  j .L.return.sub_char

# =====段结束===============
# return段标签
.L.return.sub_char:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret

  # 定义全局addx段
  .globl addx
  # 代码段标签
  .text
# =====addx段开始===============
# addx段标签
addx:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0
  # 将a0寄存器的值存入x的栈地址
  sd a0, 0(fp)
  # 将a1寄存器的值存入y的栈地址
  sd a1, 0(fp)

# =====段主体===============
# 返回语句
  # 获取局部变量y的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 跳转到.L.return.addx段
  j .L.return.addx

# =====段结束===============
# return段标签
.L.return.addx:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret

  # 定义全局add6段
  .globl add6
  # 代码段标签
  .text
# =====add6段开始===============
# add6段标签
add6:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0
  # 将a0寄存器的值存入a的栈地址
  sd a0, 0(fp)
  # 将a1寄存器的值存入b的栈地址
  sd a1, 0(fp)
  # 将a2寄存器的值存入c的栈地址
  sd a2, 0(fp)
  # 将a3寄存器的值存入d的栈地址
  sd a3, 0(fp)
  # 将a4寄存器的值存入e的栈地址
  sd a4, 0(fp)
  # 将a5寄存器的值存入f的栈地址
  sd a5, 0(fp)

# =====段主体===============
# 返回语句
  # 获取局部变量f的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量e的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量d的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量c的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量b的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量a的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 跳转到.L.return.add6段
  j .L.return.add6

# =====段结束===============
# return段标签
.L.return.add6:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret

  # 定义全局sub2段
  .globl sub2
  # 代码段标签
  .text
# =====sub2段开始===============
# sub2段标签
sub2:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0
  # 将a0寄存器的值存入x的栈地址
  sd a0, 0(fp)
  # 将a1寄存器的值存入y的栈地址
  sd a1, 0(fp)

# =====段主体===============
# 返回语句
  # 获取局部变量y的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0-a1,结果写入a0
  sub a0, a0, a1
  # 跳转到.L.return.sub2段
  j .L.return.sub2

# =====段结束===============
# return段标签
.L.return.sub2:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret

  # 定义全局add2段
  .globl add2
  # 代码段标签
  .text
# =====add2段开始===============
# add2段标签
add2:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0
  # 将a0寄存器的值存入x的栈地址
  sd a0, 0(fp)
  # 将a1寄存器的值存入y的栈地址
  sd a1, 0(fp)

# =====段主体===============
# 返回语句
  # 获取局部变量y的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 压栈,将a0的值存入栈顶
  addi sp, sp, -8
  sd a0, 0(sp)
  # 获取局部变量x的栈内地址为0(fp)
  addi a0, fp, 0
  # 读取a0中存放的地址,得到的值存入a0
  ld a0, 0(a0)
  # 弹栈，将栈顶的值存入a1
  ld a1, 0(sp)
  addi sp, sp, 8
  # a0+a1,结果写入a0
  add a0, a0, a1
  # 跳转到.L.return.add2段
  j .L.return.add2

# =====段结束===============
# return段标签
.L.return.add2:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret

  # 定义全局ret3段
  .globl ret3
  # 代码段标签
  .text
# =====ret3段开始===============
# ret3段标签
ret3:
  # 将ra寄存器压栈,保存ra的值
  addi sp, sp, -16
  sd ra, 8(sp)
  # 将fp压栈,fp属于“被调用者保存”的寄存器,需要恢复原值
  sd fp, 0(sp)
  # 将sp的值写入fp
  mv fp, sp
  # sp腾出StackSize大小的栈空间
  addi sp, sp, -0

# =====段主体===============
# 返回语句
  # 将3加载到a0中
  li a0, 3
  # 跳转到.L.return.ret3段
  j .L.return.ret3
# 返回语句
  # 将5加载到a0中
  li a0, 5
  # 跳转到.L.return.ret3段
  j .L.return.ret3

# =====段结束===============
# return段标签
.L.return.ret3:
  # 将fp的值写回sp
  mv sp, fp
  # 将最早fp保存的值弹栈,恢复fp和sp
  ld fp, 0(sp)
  # 将ra寄存器弹栈,恢复ra的值
  ld ra, 8(sp)
  addi sp, sp, 16
  # 返回a0值给系统调用
  ret
